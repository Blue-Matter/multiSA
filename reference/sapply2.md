# `sapply2` function

An alternate `sapply` function with argument `simplify = "array"` for
convenience.

## Usage

``` r
sapply2(X, FUN, ..., USE.NAMES = TRUE)
```

## Arguments

- X, FUN, ..., USE.NAMES:

  Same arguments as [`sapply()`](https://rdrr.io/r/base/lapply.html)
